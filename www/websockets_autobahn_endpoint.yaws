<erl>
out(A) -> 
    io:format("Spawning websocket owner~n",[]),
    WebSocketOwner = spawn(fun() -> yaws_websockets:websocket_owner() end),
    {websocket, WebSocketOwner, {active, once}}.
</erl>
